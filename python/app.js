function CriarDados() {
  let numeros = [1, 2, 3, 4, 5];
  let soma = numeros.reduce((a, b) => a + b);
}

function CriarDados() {
  let numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];
  let soma = numeros.reduce((a, b) => a + b);
}
